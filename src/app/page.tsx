import homeConfig from "@/configs/home/homeConfig.json";

export default function Home() {
  const { heroDesktop, heroLaptop, heroMobile, heroTitle, heroDescription } = homeConfig;

  return (
    <main>
wtf
    </main>
  )
}
